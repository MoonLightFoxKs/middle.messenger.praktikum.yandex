export enum ButtonTag {
  link = 'a',
  button = 'button',
}

export enum ButtonType {
  submit = 'submit',
  button = 'button',
}

export enum InputType {
  email = 'email',
  text = 'text',
  tel = 'tel',
  password = 'password',
}
