style. 
  :root{
    background-color: #5236c4;
  }
.chatList
  .topButtons
    !=addChatButton
    .profileButton
      !=profileButton
  .search
    !=search
  if chatPreviewList
    each chatPreview in chatPreviewList
      !=chatPreview
.chatBody
  if isNoChat
    p Выберите чат чтобы отправить сообщение
  else
    !=chat

if addChat
  !=addChatModal
